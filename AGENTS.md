# AGENTS

Refer to the README.md file.

- Pandoc to convert markdown files to folder and then index.html as `filename/index.html`.
- External Styles to be used throughly the process is at `/styles.css`. Link to it from the HTML files.
- Preserve folder structure. So, if a file `bar.md` is inside the folder `foo`, then the generated `index.html` will be at `/foo/bar/index.html`
- Create a Makefile that re-generates the whole website.
- Write the Pandoc Script to convert individual files.